export class MiseajourDto {


  etat:string="";
  result: boolean | null = null; // Initialize with null to avoid potential errors
  //result: boolean | null = null; // Initialize with null to avoid potential errors

  toLowerCase() {
    throw new Error('Method not implemented.');

  }
}
